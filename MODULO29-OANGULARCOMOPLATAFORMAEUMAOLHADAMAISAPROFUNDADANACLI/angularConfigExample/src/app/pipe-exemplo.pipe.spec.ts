import { PipeExemploPipe } from './pipe-exemplo.pipe';

describe('PipeExemploPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeExemploPipe();
    expect(pipe).toBeTruthy();
  });
});
